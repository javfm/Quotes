<p id="notice"><%= notice %></p>

<h1>Quotes</h1>
  <div class="row">
    <%= render @quotes %>
  </div>

<br>
<% if logged_in? %>
<%= link_to 'New Quote', new_quote_path %>
<% end %>
<%= paginate @quotes %>
