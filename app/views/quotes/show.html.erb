<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @quote.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @quote.content %>
</p>

<%= link_to 'Edit', edit_quote_path(@quote) %> |
<%= link_to 'Back', quotes_path %>
